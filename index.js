//need to create a mapped array that then filters through that array and returns 
//words that do not include y or Y 

var array = ['sky', 'hot', 'Yeti', 'green']
var myString = string.toLowerCase; 
var newArray = array.map(function(word) {
  return newArray.tolowercase();
}).filter(function() {
  return word;
});

//
